# Anti-Deepfake-Box Dependencies
# Install:  pip install -r requirements.txt
#
# onnxruntime note:
#   Mac/CPU  → onnxruntime     (installed below, always works)
#   Linux+GPU → also install:  pip install onnxruntime-gpu
#   The code tries onnxruntime-gpu first, falls back to onnxruntime automatically.

# ── Core deep learning ────────────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0

# ── Face detection & alignment (SSOT pipeline) ────────────────────────────────
insightface>=0.7.3
onnxruntime>=1.16.0          # CPU backend — works on Mac, Linux, Windows
                              # For GPU on Linux: pip install onnxruntime-gpu

# ── Video & audio I/O ────────────────────────────────────────────────────────
decord>=0.6.0
opencv-python-headless>=4.8.0
ffmpeg-python>=0.2.0
imageio>=2.31.0

# ── Audio processing ─────────────────────────────────────────────────────────
openai-whisper>=20231117
librosa>=0.10.0

# ── Signal processing ─────────────────────────────────────────────────────────
scipy>=1.11.0
numpy>=1.24.0

# ── Evaluation & ML utilities ─────────────────────────────────────────────────
scikit-learn>=1.3.0
Pillow>=10.0.0

# ── API & edge serving ────────────────────────────────────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
huggingface_hub>=0.19.0

# ── Configuration & CLI ───────────────────────────────────────────────────────
PyYAML>=6.0
tqdm>=4.65.0

# ── Development ───────────────────────────────────────────────────────────────
pytest>=7.4.0
